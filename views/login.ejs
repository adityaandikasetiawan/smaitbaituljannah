<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="/css/bootstrap/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            background: #f8fafc;
            font-family: 'Inter', 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }
        
        .login-container {
            background: white;
            border-radius: 16px;
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
            overflow: hidden;
            max-width: 400px;
            width: 100%;
            border: 1px solid #e2e8f0;
        }
        
        .login-header {
            background: #059669;
            color: white;
            padding: 2.5rem 2rem;
            text-align: center;
        }
        
        .login-header .icon-wrapper {
            margin-bottom: 1rem;
        }
        
        .login-header i {
            font-size: 2.5rem;
            opacity: 0.9;
        }
        
        .login-header h2 {
            margin: 0;
            font-weight: 600;
            font-size: 1.5rem;
        }
        
        .login-header p {
            margin: 0.5rem 0 0 0;
            opacity: 0.8;
            font-size: 0.9rem;
        }
        
        .login-body {
            padding: 2rem;
            display: flex;
            flex-direction: column;
            align-items: center;
        }
        
        .form-group {
            margin-bottom: 1.5rem;
            width: 100%;
            max-width: 320px;
        }
        
        .form-label {
            font-weight: 500;
            color: #374151;
            margin-bottom: 0.5rem;
            font-size: 0.9rem;
            text-align: left;
            width: 100%;
            display: block;
        }
        
        .form-label i {
            color: #6b7280;
            margin-right: 0.5rem;
        }
        
        .input-group {
            position: relative;
        }
        
        .form-control {
            border: 1px solid #d1d5db;
            border-radius: 8px;
            padding: 0.75rem 0.75rem 0.75rem 2.5rem;
            transition: all 0.2s ease;
            width: 100%;
            font-size: 0.9rem;
            background: #ffffff;
        }
        
        .form-control:focus {
            outline: none;
            border-color: #059669;
            box-shadow: 0 0 0 3px rgba(5, 150, 105, 0.1);
        }
        
        .input-icon {
            position: absolute;
            left: 0.75rem;
            top: 50%;
            transform: translateY(-50%);
            color: #9ca3af;
            font-size: 0.9rem;
        }
        
        .btn-login {
            background: #059669;
            color: white;
            border: none;
            border-radius: 8px;
            padding: 0.75rem 2rem;
            font-weight: 500;
            font-size: 0.9rem;
            cursor: pointer;
            transition: all 0.2s ease;
            width: 100%;
            max-width: 320px;
        }
        
        .btn-login:hover {
            background: #047857;
            transform: translateY(-1px);
        }
        
        .btn-login:active {
            transform: translateY(0);
        }
        
        .alert {
            background: #fef2f2;
            border: 1px solid #fecaca;
            color: #dc2626;
            padding: 0.75rem;
            border-radius: 8px;
            margin-bottom: 1rem;
            font-size: 0.85rem;
            width: 100%;
            max-width: 320px;
        }
        
        .alert i {
            margin-right: 0.5rem;
        }
        
        .links {
            margin-top: 1.5rem;
            text-align: center;
            width: 100%;
        }
        
        .links a {
            color: #059669;
            text-decoration: none;
            font-size: 0.85rem;
            margin: 0 0.5rem;
        }
        
        .links a:hover {
            text-decoration: underline;
        }
        
        .divider {
            margin: 0 0.5rem;
            color: #9ca3af;
        }
    </style>
</head>
<body>
    <div class="login-container">
        <div class="login-header">
            <div class="icon-wrapper">
                <i class="fas fa-user-circle"></i>
            </div>
            <h2>Login Siswa</h2>
            <p>Masuk ke Portal Siswa SMAIT Baituljannah</p>
        </div>
        
        <div class="login-body">
            <% if (typeof errors !== 'undefined' && errors.length > 0) { %>
                <div class="alert">
                    <i class="fas fa-exclamation-triangle"></i>
                    <% errors.forEach(error => { %>
                        <%= error %><br>
                    <% }); %>
                </div>
            <% } %>
            
            <form method="POST" action="/login" style="width: 100%; display: flex; flex-direction: column; align-items: center;">
                <div class="form-group">
                    <label class="form-label">
                        <i class="fas fa-user"></i>
                        Username
                    </label>
                    <div class="input-group">
                        <i class="input-icon fas fa-user"></i>
                        <input type="text" class="form-control" name="username" placeholder="Masukkan username Anda" 
                               value="<%= typeof formData !== 'undefined' ? formData.username || '' : '' %>" required>
                    </div>
                </div>
                
                <div class="form-group">
                    <label class="form-label">
                        <i class="fas fa-lock"></i>
                        Password
                    </label>
                    <div class="input-group">
                        <i class="input-icon fas fa-lock"></i>
                        <input type="password" class="form-control" name="password" placeholder="Masukkan password Anda" required>
                    </div>
                </div>
                
                <button type="submit" class="btn-login">
                    <i class="fas fa-sign-in-alt"></i>
                    Masuk
                </button>
            </form>
            
            <div class="links">
                <a href="/signup">Daftar Akun Baru</a>
                <span class="divider">|</span>
                <a href="/">Kembali ke Beranda</a>
                <span class="divider">|</span>
                <a href="/admin/login">Login Admin</a>
            </div>
        </div>
    </div>
</body>
</html>